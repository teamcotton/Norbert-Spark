import { describe } from 'vitest'

// Keep this file as a placeholder to avoid duplicate-transform issues
// Tests are implemented in the `.test.tsx` file. Use `describe.todo` so
// Vitest/ESLint treat this intentionally absent suite as a todo.
describe.todo('placeholder duplicate')
